export interface AccessTokenAndrefreshTokenInterface{
    accessToken: string;
    refreshToken: string;
    
}

export interface ConnectUserInterface {
    channelName: string,
    gender: string,
    target:string
}