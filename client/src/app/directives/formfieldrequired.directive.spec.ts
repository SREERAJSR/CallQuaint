import { FormfieldrequiredDirective } from './formfieldrequired.directive';

describe('FormfieldrequiredDirective', () => {
  it('should create an instance', () => {
    const directive = new FormfieldrequiredDirective();
    expect(directive).toBeTruthy();
  });
});
