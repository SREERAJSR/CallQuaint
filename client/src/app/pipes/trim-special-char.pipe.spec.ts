import { TrimSpecialCharPipe } from './trim-special-char.pipe';

describe('TrimSpecialCharPipe', () => {
  it('create an instance', () => {
    const pipe = new TrimSpecialCharPipe();
    expect(pipe).toBeTruthy();
  });
});
